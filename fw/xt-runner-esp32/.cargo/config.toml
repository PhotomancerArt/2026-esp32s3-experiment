# No --monitor in the runner: the protocol channel is binary and the client
# owns the port.
[target.xtensa-esp32-none-elf]
runner = "espflash flash --chip esp32"

[build]
rustflags = [
  "-C", "link-arg=-nostartfiles",
]

target = "xtensa-esp32-none-elf"

[unstable]
build-std = ["alloc", "core"]
